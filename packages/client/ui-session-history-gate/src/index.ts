/** Host registration for the session-history tools gate Settings row. */

/** Host plugin body — the gate's settings namespace lives on the host-side session-history-gate plugin. */
export function apply(): void {}
